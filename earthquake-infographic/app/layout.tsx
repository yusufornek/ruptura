import type { Metadata } from 'next'
import './globals.css'
import { LanguageProvider } from '@/contexts/LanguageContext'

export const metadata: Metadata = {
  title: 'RUPTURA - Disaster Management Platform',
  description: 'Uninterrupted Communication and Rapid Damage Detection in Disasters with Helium, IoT and Blockchain Technologies',
  generator: 'RUPTURA',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body>
        <LanguageProvider>
          {children}
        </LanguageProvider>
      </body>
    </html>
  )
}
